import React from "react";

const StyledMessage = () => {
  const messageStyle = {
    color: "aqua",
    fontSize: "20px",
    fontWeight: "bold",
  };

  return <p style={messageStyle}>Styled message by Kanad.</p>;
};

export default StyledMessage;
